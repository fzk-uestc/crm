<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--开启注解扫描，只扫描Controller注解-->
    <context:component-scan base-package="com.fzk.crm.settings">
        <!--只扫描Controller注解-->
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>
    <context:component-scan base-package="com.fzk.crm.workbench">
        <!--只扫描Controller注解-->
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>


    <!-- 设置静态资源不过滤 -->
    <mvc:resources location="/image/" mapping="/image/**"/>  <!-- 图片 -->
    <mvc:resources location="/jquery/" mapping="/jquery/**"/>  <!-- javascript -->
    <mvc:resources location="/ECharts/" mapping="/ECharts/**"/>  <!-- javascript -->
    <mvc:resources location="/settings/" mapping="/settings/*.html"/>  <!-- html -->
    <mvc:resources location="/workbench/main/" mapping="/workbench/**.html"/>  <!-- html -->
    <!--开启springmvc注解的支持-->
    <mvc:annotation-driven enable-matrix-variables="true"/>
</beans>